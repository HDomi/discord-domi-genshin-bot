{
  "name": "domi-genshin-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "docker build -t domi-genshin-bot .",
    "run": "docker run --env-file .env.development -p 5540:5540 domi-genshin-bot",
    "clean": "docker stop $(docker ps -q --filter ancestor=domi-genshin-bot) 2>/dev/null || true && docker rm $(docker ps -a -q --filter ancestor=domi-genshin-bot) 2>/dev/null || true && docker rmi domi-genshin-bot 2>/dev/null || true",
    "rebuild": "npm run clean && npm run build && npm run run",
    "del-images": "docker rmi $(docker images -q)",
    "del-containers": "docker rm $(docker ps -a -q)"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@discord-player/extractor": "^7.1.0",
    "@discordjs/opus": "^0.10.0",
    "@discordjs/voice": "^0.18.0",
    "@napi-rs/canvas": "^1.0.0",
    "discord-player": "^7.1.0",
    "discord-player-youtubei": "^1.3.2",
    "discord.js": "^14.16.1",
    "dotenv": "^16.0.1",
    "ffmpeg-static": "^5.2.0",
    "firebase": "^12.0.0",
    "genshin-db": "^5.2.11",
    "mediaplex": "^1.0.0",
    "nodemailer": "^6.10.0",
    "steamapi": "^3.0.12"
  }
}